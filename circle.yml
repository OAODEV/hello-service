machine:
  services:
    - docker
  environment:
    herd_build_tag: $(cat $CIRCLE_PROJECT_REPONAME/Version)_build.$(echo $CIRCLE_SHA1 | cut -c1-7)

dependencies:
  override:
    - echo $herd_build_tag
    - docker info
    - docker build -t r.iadops.com/hello:$herd_build_tag .

test:
  override:
    - docker run r.iadops.com/hello:$herd_build_tag python test_hello_service.py

#deployment:
#  index:
#    branch: /.*/
#    commands:
#      - docker push r.iadops.com/hello:$herd_build_tag